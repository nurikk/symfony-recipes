name: Update Flex endpoint

permissions:
  contents: write

on:
  push:
    branches:
      - master

defaults:
  run:
    shell: bash

jobs:
  call-flex-update:
    uses: ./.github/workflows/callable-flex-update.yml
    with:
      contrib: true
      branch: master
